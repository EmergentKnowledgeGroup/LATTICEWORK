{
  "name": "@latticework/phase3-browser",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "vite build --config vite.config.ts",
    "test": "playwright test",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@latticework/contracts": "0.0.0",
    "@latticework/storage": "0.0.0"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "vite": "8.1.5"
  }
}
