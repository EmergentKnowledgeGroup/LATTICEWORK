# Handoff — `LW-P4-001`

**From:** Codex root controller
**To:** Next Phase 5 planner
**Date:** `2026-07-31 America/Chicago`
**Current commit:** `e65ca81940d50eabd5bb72a403deab3bf37bea93`
**Branch:** `reengineering/p4-characterization-retests`

## State in one paragraph

The accepted non-default `/p4.html` synthetic Chat slice is implemented and
canonical plus clean detached-worktree verification is GREEN after the single
CodeRabbit review and one consolidated fix pass. It adds no real data,
credentials, provider traffic, application listener, activation, deployment,
or cutover.

## Completed

- Implemented typed Chat contracts, serialized controller state, candidate-only
  persistence, deterministic in-process mocks, a removable Lit workbench, and
  bounded browser/fixture verification.
- Hardened repository failure settlement, send/send and send/hydrate ordering,
  verifier-selected ports, clean profile teardown, and derived browser totals.
- Promoted canonical and independent receipts under
  `reengineering/evidence/phase-4/LW-P4-001/`.

## Verified

- 63/63 targeted assertions and 187/187 repository controls pass.
- Seven browser cases pass and the sole offline-contract case is explicitly
  skipped because no service worker is authorized.
- Typecheck, deterministic builds, audit, SBOM, supply-chain, hygiene, scope,
  protected-boundary, and detached-worktree gates are GREEN.
- Independent review is
  `reengineering/evidence/phase-4/LW-P4-001/independent-review/REVIEW.md`.

## Single-review disposition

- The one CodeRabbit review completed with 14 inline findings and 20 smaller
  hardening notes. No second review was requested.
- The consolidated fix pass removed the stray empty-state glyph, guarded empty
  keyboard submission, surfaced streamed deltas, preserved unrelated
  conversation records, bounded the missing-signal cancellation branch,
  synchronized cancellation without sleeps, passed Playwright `baseURL`,
  made fixture close retry-safe, pinned Node types, serialized browser workers,
  added provider-exhaustion and scope-continuation controls, and made listener
  validation errors independently identifiable.
- The request to add `/p4.html` to the production build was rejected because
  `production_build_authorized` is false and `apps/web/vite.config.ts` is a
  protected path.
- Suggestions against `tests/characterization/**` and the closed amendment
  promoter/validator were not changed in this implementation workstream:
  those paths are explicitly protected or unowned by the accepted packet.
  They remain review notes for a separately claimed control-plane work unit.
- The fixture request-path nit was not implemented because the packet's source
  gate intentionally forbids request-capable APIs outside the sole listener
  file. The existing fixture behavior remains bounded and its lifecycle gate
  is covered.

## Not verified

- Real conversation data or migration.
- Real provider protocols, credentials, or traffic.
- Offline service-worker behavior.
- Candidate activation, deployment, default-route cutover, or legacy parity.

## Changed files

- Exact paths frozen by `reengineering/PHASE4_IMPLEMENTATION_PACKET.md`.
- `reengineering/evidence/phase-4/LW-P4-001/`.
- Living state, testing, compatibility, architecture, checklist, claim,
  handoff, and checkpoint files.

## Important relationships

- `/` remains the legacy/default route; `/p4.html` is isolated and non-default.
- Deterministic collectors/contracts own state truth; provider mocks never gain
  transport or credential capability.
- Per-conversation hydration and writes share one serialization queue.

## Known risks

- Offline behavior remains unclaimed.
- Proof is Windows/installed-Chrome and synthetic/mock only.
- Broader data, network/security, and cutover blockers remain open.

## Do not do next

- Do not activate `/p4.html`, read real stores, add provider traffic, introduce
  a listener, or change default/legacy/deployment paths without a new accepted
  packet.

## Read first

1. `PROJECT_STATE.md`
2. `reengineering/PHASE4_IMPLEMENTATION_PACKET.md`
3. `reengineering/evidence/phase-4/LW-P4-001/summary.json`

## Next exact action

Resolve the single review's threads, merge PR #5, then write the post-merge
checkpoint.

## Success condition

The PR merges with required checks green and all review threads resolved,
without a second CodeRabbit review request or any forbidden capability.

## Resume command

```powershell
git status --short --branch
```
