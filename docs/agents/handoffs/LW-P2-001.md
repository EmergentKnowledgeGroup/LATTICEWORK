# Handoff - `LW-P2-001`

**From:** Codex root controller
**To:** Phase 3 maintainer decision gate
**Date:** `2026-07-30T11:26:02Z`
**Current candidate commit:** `c8a040fb38f627bf4d0353b3497645653a57139c`
**Branch:** `reengineering/m0-baseline-characterization`

## State in one paragraph

Phase 2 is complete and independently verified. LATTICEWORK now has an
isolated strict-TypeScript contracts/kernel foundation and a local-only Lit
status shell with a guarded relative build. The canonical and independent
gates prove only this feature-free seam. The preserved legacy runtime remains
authoritative, no legacy feature or stored data was migrated, no route changed,
and the repository is not release- or cutover-ready.

## Completed

- Added exact-pinned npm workspaces for `apps/web`, `packages/contracts`,
  `packages/kernel`, and isolated Phase 2 browser tests.
- Implemented lifecycle, diagnostics, and status contracts.
- Implemented dependency-injected deterministic start/reverse-stop kernel
  behavior with safe failure diagnostics.
- Implemented an accessible candidate-only Lit status shell.
- Added guarded build/output handling, deterministic build comparison,
  protected-file verification, supply-chain/SBOM capture, and strict evidence
  validation.
- Added browser denial for out-of-origin requests, realtime channels, workers,
  EventSource, `sendBeacon`, storage, and service workers.
- Preserved canonical and independently reproduced evidence with manifests.

## Verified

- Canonical candidate: `c8a040fb38f627bf4d0353b3497645653a57139c`.
- Strict typecheck passed for all three implementation workspaces.
- Kernel unit tests passed 5 of 5.
- Repository-control tests passed 37 of 37.
- Two clean builds emitted byte-identical declared files and hashes.
- Candidate browser tests passed 6 of 6 with zero skipped, unexpected, or
  flaky tests.
- The canonical evidence validator reported `valid: true`, 16 command
  receipts, eight protected paths, eight required browser artifacts, and zero
  failures.
- npm audit reported zero vulnerabilities; lockfile replay was byte-identical;
  CycloneDX and license/integrity receipts are preserved.
- Independent QA reproduced the complete gate from a separate Z:-local
  directory on loopback port 4177 and accepted `LW-P2-001`.
- Evidence:
  `reengineering/evidence/phase-2/LW-P2-001/`.

## Not verified

- Any migrated legacy feature, route, provider, or stored-data behavior.
- Local or cloud model connections, chat send/stream/cancel/retry, import or
  export, identity, cryptography, service-worker update, gateway, worker,
  peer-to-peer, desktop, GPU, audio, or external mutation.
- Cross-browser/OS parity, complete screen-reader accessibility, equivalent
  feature performance, deployment generation, release packaging, or cutover.

## Changed files

- `package.json`, `package-lock.json`, and `tsconfig.base.json`.
- `apps/web/**`.
- `packages/contracts/**`.
- `packages/kernel/**`.
- `tests/phase2/**`.
- Phase 2 tools and control tests under `tools/reengineering/**` and
  `tests/reengineering/**`.
- `reengineering/evidence/phase-2/LW-P2-001/**`.
- Evidence-linked living documents, claim, handoff, inventory, and checkpoint
  ledgers.

## Important relationships

- `apps/web/src` is the sole authored candidate browser source; its generated
  output remains ignored under `output/lw-p2-001/web/`.
- Contracts do not depend on features or adapters; the kernel depends only on
  contracts; the shell depends only on the kernel/contracts boundary and Lit.
- The immutable baseline at `e7585999...` and all eight protected legacy paths
  remain byte-untouched.
- Candidate evidence is not a parity claim. Compatibility levels remain
  bounded to their existing Phase 0/1 observations.

## Known risks

- The candidate shell can be mistaken for a replacement despite containing no
  migrated features.
- Legacy root/`docs` source precedence remains unresolved until later
  generation/cutover work.
- Phase 3 data semantics, provider trust boundaries, and rollback rules still
  require accepted decisions.

## Do not do next

- Do not connect the candidate to legacy storage or providers by convenience.
- Do not mutate a persistence format, unknown field, credential, identity,
  cryptographic value, external service, deployment mirror, or default route
  without the required ADR and work claim.
- Do not describe Phase 2 as feature parity, a drop-in rewrite, or release
  readiness.

## Read first

1. `PROJECT_STATE.md`
2. `reengineering/BLOCKERBOARD.md`
3. `reengineering/DATA_INVENTORY.md`
4. `reengineering/DATA_PRESERVATION_REGISTRY.json`
5. `reengineering/SECURITY_BOUNDARY_MAP.md`

## Next exact action

Draft the bounded Phase 3 data/storage and provider-contract decision packet,
then obtain explicit maintainer acceptance before implementation.

## Success condition

The next accepted decision defines schema ownership, versioning,
unknown-field preservation, migration/rollback, provider boundaries, and
executable verification without changing legacy data or external behavior.

## Resume command

```powershell
Get-Content -Raw docs/agents/handoffs/LW-P2-001.md
```
