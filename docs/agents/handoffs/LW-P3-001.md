# Handoff — `LW-P3-001`

**From:** Codex root controller
**To:** Phase 4 planning controller
**Date:** `2026-07-30T16:01:29Z`
**Current candidate commit:** `d746b96225a3eaf59a5b5937e3f531e2cad280ef`
**Branch:** `reengineering/p3-storage-provider-foundation`

## State in one paragraph

Phase 3's first additive storage/provider foundation is complete and
independently verified. The candidate implements a synthetic-only,
copy-on-write conversation repository/migration seam and deterministic
in-process mock-provider routing/provenance seam. All 12 frozen gates are
GREEN. The packages remain unused by the application; legacy runtime and data
remain authoritative, and no real data, provider traffic, listener, feature,
activation, or cutover is authorized.

## Completed

- Added strict storage and provider contracts to `@latticework/contracts`.
- Added `@latticework/storage` with read-only source access, inactive candidate
  namespaces, copy-on-write checkpoint/resume/rollback, immutable journaling,
  source identity/equivalence, hostile staging, and fail-closed native-value
  verification.
- Added `@latticework/providers` with immutable capability-bound grants,
  synthetic credential references, deterministic mock adapters, explicit
  routing/retry/fallback policy, normalized terminal behavior, and
  content-free provenance.
- Added native Chromium IndexedDB coverage, provider no-egress controls,
  protected-boundary verification, evidence validation, and one canonical
  verification runner.
- Finalized canonical evidence and attached a separate clean-worktree
  independent review.

## Verified

- Exact candidate:
  `d746b96225a3eaf59a5b5937e3f531e2cad280ef`.
- All 12 frozen gates in `reengineering/PHASE3_PREFLIGHT.json` are valid.
- Six workspace typechecks pass.
- Storage tests pass 26 of 26; provider tests pass 19 of 19; boundary tests
  pass 6 of 6; evidence-validator controls pass 10 of 10.
- Native Chromium IndexedDB scenarios pass 5 of 5.
- Full repository controls pass 107 of 107 with zero fail, skip, or todo.
- Two candidate builds and isolated lockfile replay are byte-identical.
- npm audit reports zero vulnerabilities; SBOM, supply-chain, protected-path,
  no-egress, JSON, parser, diff, and whitespace gates are valid.
- The canonical manifest hashes 138 artifacts.
- Independent clean-worktree QA is GREEN with no findings.
- Evidence:
  `reengineering/evidence/phase-3/LW-P3-001/`.

## Not verified

- Any real user-data read, migration, activation, cleanup, or cutover.
- Real credentials, provider endpoints, protocols, traffic, proxy, or
  listener behavior.
- Runtime registration, feature integration, Chat or conversation behavior,
  route selection, or provider activation.
- Legacy feature parity, import/export compatibility, broader browser/OS
  behavior, deployment, release packaging, production readiness, or cutover.

## Changed files

- `packages/contracts/src/{index,storage,provider}.ts`.
- `packages/storage/**` and `packages/providers/**`.
- `tests/phase3/**` and the Phase 3 reengineering controls.
- `tools/reengineering/run-phase3-verification.ps1`,
  `verify-phase3-boundary.mjs`, and `validate-phase3-evidence.mjs`.
- `package.json` and `package-lock.json`.
- `reengineering/evidence/phase-3/LW-P3-001/**`.
- Evidence-linked living documents, claim, handoff, execution ledger,
  blockerboard, and checkpoint ledgers.

## Important relationships

- The source reader is injected and read-only; migrated data is copied only
  into inactive `latticework::*` candidate namespaces.
- Source identity and candidate equivalence use one shared fail-closed native
  structured-clone contract.
- Egress grants are immutable and checked before synthetic credential
  resolution; providers are deterministic in-process mocks with no transport.
- The feature-free Phase 2 shell and all legacy runtime paths do not import the
  Phase 3 packages.
- The immutable upstream baseline remains
  `e7585999fc1af2707f410ae87356cf2b52e08d9c`.

## Known risks

- The verified synthetic conversation descriptor does not establish the
  owners, schemas, retention, or migration behavior of other legacy datasets.
- Native structured-clone values outside the explicitly supported contract
  abstain; broader browser/OS equivalence is unknown.
- Provider mocks do not characterize real APIs, credentials, transports,
  callback timing, billing, or external failure behavior.
- The presence of verified packages can be mistaken for runtime integration;
  no integration or activation exists.

## Do not do next

- Do not point the source reader at real browser profiles or stores.
- Do not add a provider endpoint, credential, listener, proxy, or ambient
  environment read under this claim.
- Do not import these packages into `apps/web` or a legacy feature by
  convenience.
- Do not describe this evidence as feature parity, migration completion,
  activation, production readiness, or cutover authority.

## Read first

1. `PROJECT_STATE.md`
2. `reengineering/evidence/phase-3/LW-P3-001/summary.json`
3. `reengineering/evidence/phase-3/LW-P3-001/independent-review/REVIEW.md`
4. `reengineering/BLOCKERBOARD.md`
5. `docs/decisions/0004-versioned-storage-and-migration.md`
6. `docs/decisions/0005-provider-abstraction-and-provenance.md`
7. `docs/decisions/0006-optional-local-proxy-security.md`

## Next exact action

Draft and independently review a separately claimed Phase 4 vertical-slice
preflight. It may depend on the verified Phase 3 contracts but must not activate
real storage or provider behavior without the missing decisions and evidence.

## Success condition

The next accepted work unit names one bounded feature slice, exact compatibility
surfaces, real/synthetic authority, rollback, stop conditions, and executable
gates without silently activating Phase 3 packages.

## Resume command

```powershell
Get-Content -Raw docs/agents/handoffs/LW-P3-001.md
```
