# Work Claim — `LW-M0-001`

**Status:** Completed
**Owner:** Codex root controller
**Coordinator:** Codex root controller
**Base commit:** `e7585999fc1af2707f410ae87356cf2b52e08d9c`
**Branch:** `reengineering/m0-baseline-characterization`
**Claim time:** `2026-07-30T07:51:41Z`

## Objective

Install the canonical LATTICEWORK documentation pack onto the fork lineage and establish a reproducible, immutable Phase 0 baseline without changing upstream runtime behavior.

## In scope

- Fork/provenance verification and immutable baseline tag/worktree.
- Required checkpoint and reengineering control artifacts.
- Baseline source, test, dependency, data, network, launch-mode, and capability inventories.
- Raw Phase 0 evidence and machine-readable manifests.
- Initial architecture decision proposals for canonical source, module architecture, and UI strategy.
- Living-document updates that record measured baseline truth.

## Out of scope

- Runtime behavior changes.
- Feature removal or retirement.
- Stored-data, privacy, security, identity, or cryptographic semantic changes.
- Broad framework migration.
- Public compatibility, readiness, or benchmark-victory claims.

## Locked constraints

- Preserve upstream history and the exact upstream `LICENSE`.
- Characterize only the detached baseline worktree at the pinned SHA.
- Do not weaken, skip, quarantine, or delete baseline tests.
- Do not move legacy files before a preservation manifest, rollback proof, and owner approval.
- Keep `Z:\FreeLattice` untouched.

## Compatibility surfaces

- All surfaces listed in `docs/COMPATIBILITY.md`.
- All launch modes and user-reachable behavior frozen by `reengineering/BASELINE_CAPABILITY_CONTRACT.md`.

## Files expected to change

- Canonical and living documentation installed from the LATTICEWORK pack.
- `reengineering/**`
- `runtime/checkpoints/**`
- `docs/agents/claims/**`
- `docs/agents/sessions/**`
- `docs/agents/handoffs/**`
- `artifacts/**`
- Phase 0 tooling only when required to produce reproducible evidence.

## Files not to change

- Existing upstream runtime source and deployment artifacts.
- Existing upstream tests, except additive characterization harnesses after the baseline manifest is frozen.
- `LICENSE`

## Acceptance criteria

- Baseline commit, tag, detached worktree, archive hash, tree hash manifest, and environment are recorded.
- Existing smoke suite is archived with exact exit code and pass/fail counts.
- Required Phase 0 control artifacts exist and contain evidence-linked current truth.
- Canonical source ambiguity and launch-mode obligations are explicitly mapped.
- ADR-001 through ADR-003 are proposed, not silently accepted.
- No upstream runtime behavior file changes.

## Required tests

- `node tests/smoke.js` in the detached baseline worktree.
- JSON parse checks for machine-readable control/evidence files.
- Hash verification for preservation artifacts.
- Documentation-link and placeholder checks.

## Required documentation updates

- `PROJECT_STATE.md`
- `PROVENANCE.md`
- `docs/BASELINE.md`
- `docs/ARCHITECTURE.md`
- `docs/COMPATIBILITY.md`
- `docs/TESTING_AND_VERIFICATION.md`
- Relevant reengineering control artifacts.

## Risks

- The upstream smoke suite is known to exit non-zero; that result must be preserved rather than “fixed” during Phase 0.
- Baseline capability and data inventories may expose P0/P1 ambiguity that blocks implementation.
- The canonical pack contains a line-ending-different `LICENSE`; the exact upstream file is preserved instead.

## Escalation trigger

Stop and request decision when:

- Canonical source, target UI strategy, or preservation boundaries remain ambiguous after Phase 0 evidence.
- Work would alter a sensitive boundary or reduce a baseline obligation.
- An ADR requires maintainer acceptance before implementation.
