# Work Claim — `LW-P4-001`

**Status:** Verified — canonical and independent GREEN
**Owner:** Codex root controller
**Base commit:** `212b98b00a4aac9fad91b8a083c382cbd64e3ee4`
**Implementation comparison base:** `faf32dbaf8159e8499421fa68d9fba4bede0fdc9`
**Branch:** `reengineering/p4-characterization-retests`
**Claim time:** `2026-07-31T03:42:00Z`
**Verified candidate:** `cb94b608a7b0c552154ec01a44bda9fa1ea28ec1`

## Objective

Implement and fully verify the exact accepted, removable, non-default,
synthetic/mock primary-Chat vertical slice at `/p4.html`.

## In scope

- Exact paths and prefixes in the accepted
  `reengineering/PHASE4_IMPLEMENTATION_PACKET.md` machine lock.
- Chat contracts/controller, deterministic mock composition, synthetic
  candidate persistence, non-default Lit UI, exact test-only stream fixture,
  tests, verification tooling, evidence, living docs, and checkpoints.

## Out of scope

- Real data, credentials, provider traffic, source readers, migration,
  activation, deployment, cutover, or application listener.
- `/`, default build input, shared/global UI, protected Phase 3 packages,
  legacy runtime, service workers, servers, desktop, worker, or deployment.

## Intended invariant

The user message persists before dispatch; assistant content persists only
after a completed terminal. Cancellation/failure persists only content-free
terminal metadata, with one terminal, zero retry, zero fallback, and rejected
late events. `/` and every protected path remain byte-identical to the accepted
implementation base.

## Acceptance

- All packet verification gates pass locally and from a clean detached
  worktree.
- Desktop/mobile/a11y/offline/reload/cancel/diagnostic browser evidence is
  complete and visually inspected.
- Exact-loopback test listener teardown/port release and zero external egress
  are proven.
- Independent QA is GREEN; one PR receives one CodeRabbit review, one fix pass,
  and merges cleanly.

## Stop conditions

Stop for real data/credential/provider access, protected/default path drift,
application listener, activation/deployment/cutover, unproven cleanup, or any
scope expansion beyond the accepted packet.
