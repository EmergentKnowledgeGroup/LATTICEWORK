# Work Claim — `LW-P3-001`

**Status:** Completed — VERIFIED bounded synthetic/mock foundation
**Owner:** Codex root controller
**Coordinator:** Codex root controller
**Base commit:** `93a36626f786a880210c53b8486c961e8b86e9ea`
**Branch:** `reengineering/p3-storage-provider-foundation`
**Claim time:** `2026-07-30T14:03:39Z`

## Authority receipt

**OBSERVED:** after reviewing the approved choices, the maintainer replied
`approved choices - continue` in the active Codex task on 2026-07-30.

This claim records that reply as acceptance of ADR-004, ADR-005, and ADR-006.
It authorizes only the exact `reengineering/PHASE3_PREFLIGHT.*` bounded
execution surface. ADR-006 acceptance freezes a future optional-proxy contract
but authorizes no Phase 3 listener.

## Objective

Implement and independently verify the first additive Phase 3 foundation:

1. synthetic-only, copy-on-write conversation storage and migration contracts;
2. deterministic in-process local/cloud provider mocks, routing, policy,
   credential-reference, cancellation, and provenance contracts.

The implementation remains unused by the candidate app and legacy runtime.

## Intended invariant

The slice proves contract behavior only against synthetic fixtures and
in-process mocks. It does not read or mutate real user data, resolve a real
credential, contact or probe a real provider, start a listener, register into
Chat or another feature, change a route, activate a migrated dataset, or
authorize cutover.

## Exact owned paths

- `package.json`
- `package-lock.json`
- `packages/contracts/src/index.ts`
- `packages/contracts/src/storage.ts`
- `packages/contracts/src/provider.ts`
- `packages/storage/**`
- `packages/providers/**`
- `tests/phase3/**`
- `tests/reengineering/phase3-storage-boundary.test.mjs`
- `tests/reengineering/phase3-provider-boundary.test.mjs`
- `tests/reengineering/phase3-evidence-validation.test.mjs`
- `tools/reengineering/run-phase3-verification.ps1`
- `tools/reengineering/verify-phase3-boundary.mjs`
- `tools/reengineering/validate-phase3-evidence.mjs`
- `reengineering/evidence/phase-3/LW-P3-001/**`
- this claim and `docs/agents/handoffs/LW-P3-001.md`
- evidence-linked living documents and both checkpoint surfaces listed in the
  preflight

## No-touch fence

- `app.html`, `index.html`, `docs/app.html`
- `modules/**`, `docs/modules/**`
- `sw.js`, `docs/sw.js`
- `server.js`, `server.py`, `desktop/**`, `worker/**`,
  `telegram-worker.js`
- `tests/smoke*`, `tests/characterization/**`, `apps/web/**`
- `Z:\LATTICEWORK_BASELINE_e7585999`, `Z:\FreeLattice`
- real browser profiles, real stores, credentials, provider endpoints,
  deployment mirrors, default routes, and legacy feature registration

## Compatibility surfaces

- `FreeLatticeDB` v3 schema metadata for `conversations` and `messages`
- unknown structured-clone values in the authorized synthetic dataset
- migration journal, import/export, staging, rollback, and source-unchanged
  behavior
- operation/attempt identity, event ordering, normalized errors, retry,
  cancellation, credential/egress binding, callback-once, and provenance
- all 252 pinned data-preservation obligations remain a minimum floor

## Required verification

Follow all 12 gates in `reengineering/PHASE3_PREFLIGHT.json`, including strict
typecheck, Node unit/contract tests, real-browser synthetic IndexedDB tests,
zero-egress/provider controls, protected-boundary comparison, deterministic
build/lock replay, supply-chain receipts, full controls, evidence validation,
independent clean-worktree reproduction, and hygiene.

## Stop conditions

Stop if implementation needs a real dataset, credential, provider endpoint,
listener, new dependency outside the accepted exact pins, legacy/app path,
runtime registration, activation, cutover, weakened preservation rule, or
evidence containing private content.

## Terminal condition

Complete only when the canonical evidence bundle is valid, independent QA is
GREEN, the implementation branch is review-ready, and no claimed compatibility
extends beyond synthetic storage and deterministic mock-provider contracts.

## Completion receipt

**VERIFIED:** candidate
`d746b96225a3eaf59a5b5937e3f531e2cad280ef` satisfies all 12 frozen Phase 3
gates. Canonical evidence is stored under
`reengineering/evidence/phase-3/LW-P3-001/`; the separate clean-worktree review
is GREEN with no findings.

This closes only the claimed synthetic/mock contract slice. It does not
authorize real-data access, real credentials or provider traffic, a listener,
runtime registration, feature integration, activation, cutover, or a
production-readiness claim.
