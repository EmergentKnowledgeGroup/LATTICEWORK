# ADR Path Compatibility Note

The canonical Architecture Decision Record home is [`../decisions/`](../decisions/), as defined by `DOCS_MANIFEST.md`.

The reengineering goal prompt referenced `docs/adr/`. This directory exists only as a routing alias so contributors do not create a second decision ledger.
